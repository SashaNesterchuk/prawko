# Feature: Onboarding
# Scenario: New user completes onboarding and lands on Home (first-start spotlight is currently off)
appId: com.mindjar.prawko
tags:
  - smoke
  - onboarding
---
- runFlow: subflows/complete_onboarding.yaml

- assertVisible:
    id: "screen-home"
- assertVisible:
    id: "tab-index"
- assertNotVisible:
    id: "home-start-spotlight"
- assertVisible:
    id: "action-tile-trainer"
- assertVisible:
    id: "home-tile-blitz"
